run:
  dir: ${core.log_dir}/runs/${core.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}

sweep:
  dir: ${core.log_dir}/multiruns/${multirun_name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
  subdir: ${hydra.job.num}-${core.name}
